# Derived Laws

- RuleArtifact != GeneratorSpec != NormativeProposition.
- Admission truth != evidence/authenticity/provenance of admission.
- FormalPrimitive != SemanticPrimitive != OnticPrimitive.
- NormativePower != physical capability != ordinary permission != effective control.
- Within-context amendment is predecessor-state-relative by default; genesis/replacement is distinct from valid amendment.
- Identity != continuity != succession != transfer != efficacy != validity.
- NormativeReason != MotivatingReason != ExplanatoryReason.
