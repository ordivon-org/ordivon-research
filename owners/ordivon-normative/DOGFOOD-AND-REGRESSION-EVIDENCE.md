# Dogfood & Regression Evidence

Status: bounded evidence for the current Formal Core Reference Contract v1.

## Frozen synthetic corpus

Current executable fixture families:

- NCX: 16 core anti-collapse/counterexample fixtures;
- NCF: 6 support/standing-calculus fixtures;
- N3F: 8 conflict/exception/defeat fixtures;
- N4F: 7 temporal-standing/lifecycle fixtures;
- N5F: 9 power/delegation/succession fixtures;
- N6F: 7 multi-context/jurisdiction/translation fixtures;
- GDF: 8 Game-first semantic fixtures;
- HAG: 5 executable Harness/Agent boundary checks.

The non-authoritative conformance witness currently replays **66/66 PASS**. This execution result is evidence about conformance of one disposable backend, not semantic authority.

## Actual Game dogfood

Authoritative Game inputs: current GDF0–GDF3 consolidation and materialized Game research core.

Result: **PASS / zero CoreFailure**.

Key findings:

- GDF0 EffectiveRuleTopology remains Game-owned domain rule topology; Normative supplies general admission/effectivity/applicability/support/assessment semantics without annexing Game content.
- GDF3 AuthoritativeCaseDetermination decomposes at owner-general level into typed target/status + supported Power + predecessor-relative ValidChange + determination basis/support + temporal binding/currentness. No universal Determination primitive was forced.
- GPR role/authority/contest/evidence/finality/remedy artifacts are useful Game practical/consumption views, not Normative ontology objects.
- C1 GameInstance Constitution/Continuity pressures context/instance representation but does not force one Context identity model.

## Actual Harness / Agent authority dogfood

Authoritative Harness input: current HaF0–HaF61 consolidation/family architecture.

Result: **PASS / zero CoreFailure**.

Key findings:

- Harness mediates externally grounded authority/policy/commitment/process state but does not generate legitimacy, obligation, fairness, entitlement, remedy correctness, or normative finality.
- `Authorized != Eligible != Admitted != Scheduled != Dispatched != Executing != Effected != Completed != Final` remains operational mediation structure, not one normative lifecycle.
- `ToolIntent != PhysicalEffect != SemanticSuccess` directly supports the Normative fact/effect/validity boundary.
- Composite authorization is representable as support composition and context-defined standing-sufficiency conditions; no universal authorization object/algebra was forced.
- Evidence/assurance may enter support bases but does not self-create standing.
- Agent/Run/model/tool continuity does not self-transfer normative authority.
- User/org/platform/provider/domain overlays remain independent contexts unless an explicit receiving/bridge-context basis relates them.

## Executable witness evidence

Witness path: `conformance-witness/`.

Final regression execution evidence is recorded in `conformance-witness/RESULTS.md`. The witness is explicitly NON-AUTHORITATIVE / NON-CANONICAL / NON-PRODUCTION / DISPOSABLE.

A green witness run means only that the explicit fixture encoding is reproduced by the admitted thin calculus. It does not establish domain truth, choose current authority, or authorize actions.

## Failure policy

Classify future dogfood results as:

- `VocabularyExtension` — domain-specific typed claims instantiate the existing open claim vocabulary;
- `ModelPressure` — a representation becomes awkward but core semantics still expresses the distinction;
- `CoreFailure` — a required normative distinction cannot be expressed without violating the frozen formal contract.

Only a concrete CoreFailure (or evidence that an existing obligation is unnecessarily strong) reopens the formal core.
